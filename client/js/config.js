// Haven Space Configuration

const CONFIG = {
  // Backend API URL - Adjust based on your XAMPP/Apache setup
  //
  // Common configurations:
  // 1. If project folder is in Apache's htdocs as 'havenspace-website':
  //    API_BASE_URL: 'http://localhost/havenspace-website/Haven-Space/server/api'
  // 2. If project folder is in Apache's htdocs as 'Haven-Space':
  //    API_BASE_URL: 'http://localhost/Haven-Space/server/api'
  // 3. If Apache is configured with project root as DocumentRoot:
  //    API_BASE_URL: 'http://localhost/server/api'

  API_BASE_URL: 'http://localhost:8000',
};

export default CONFIG;
